type ColorProps ={
    color: string;
};


type AnimalContextProps = {
    species : string,
    color : string,
    
};

type ShoppingItemProps = {
    id: number;
    name: string;
};

type PointsProps = {
    startCounter: number;
};